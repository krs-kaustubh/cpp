#include <bits/stdc++.h>

int multiply(int a, int b);

int main()
{
    int a, b;
    std::cout << "Enter two numbers: ";
    std::cin >> a >> b;
    std::cout << "Product: " << multiply(a, b) << std::endl;
    return 0;
}